import { test } from "@playwright/test";

// Replace these with real E2E tests once the game server is running.
// Run: npx serve src/ -p 3000, then npm run test:e2e
test.skip("placeholder — spin triggers reel animation", () => {});
test.skip("placeholder — win updates balance display", () => {});
test.skip("placeholder — mute button toggles audio", () => {});
