{
  "name": "broke-college-slot-machine",
  "version": "1.0.0",
  "private": true,
  "description": "Clean code enforcement for the broke college slot machine project.",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "ci": "npm run lint && npm run format:check"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.0",
    "eslint": "^9.25.0",
    "globals": "^16.0.0",
    "prettier": "^3.5.3"
  }
}
